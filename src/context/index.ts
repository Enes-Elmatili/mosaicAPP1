// On exporte le Provider React (gestion login/logout/checkAuth)
export { AuthProvider } from "./AuthContext";

// On exporte le hook + contexte (accès user, isAuthenticated, etc.)
export { useAuth } from "./auth-context";
