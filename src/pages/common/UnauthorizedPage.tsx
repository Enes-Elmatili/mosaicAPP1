import React from 'react';
import PageWrapper from '../../components/Layout/PageWrapper';

const UnauthorizedPage: React.FC = () => <PageWrapper title="Non autorisé" />;

export default UnauthorizedPage;
