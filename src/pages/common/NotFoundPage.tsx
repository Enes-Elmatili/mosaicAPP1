import React from 'react';
import PageWrapper from '../../components/Layout/PageWrapper';

const NotFoundPage: React.FC = () => <PageWrapper title="Page non trouvée" />;

export default NotFoundPage;
